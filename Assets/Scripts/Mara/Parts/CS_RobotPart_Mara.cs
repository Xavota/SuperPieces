using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CS_RobotPart_Mara
{
  // Start is called before the first frame update
  public virtual void Start(CS_RobotPartsManager_Mara manager) { }

  // Update is called once per frame
  public virtual void Update(CS_RobotPartsManager_Mara manager) { }

  // Update is called once per frame
  public virtual void FixedUpdate(CS_RobotPartsManager_Mara manager) { }
}
